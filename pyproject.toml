[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "silver-price-calculator"
version = "1.0.0"
description = "Silver Price Calculator & Silver Sales Dashboard"
readme = "README.md"
requires-python = ">=3.9"
